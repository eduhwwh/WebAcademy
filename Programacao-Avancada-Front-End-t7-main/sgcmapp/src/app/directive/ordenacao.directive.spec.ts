import { OrdenacaoDirective } from './ordenacao.directive';

describe('OrdenacaoDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdenacaoDirective();
    expect(directive).toBeTruthy();
  });
});
